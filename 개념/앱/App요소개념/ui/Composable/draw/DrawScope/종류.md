| 함수                 | 기능                |
| ------------------ | ----------------- |
| `drawRect()`       | 사각형               |
| `drawRoundRect()`  | 둥근 사각형            |
| `drawCircle()`     | 원                 |
| `drawOval()`       | 타원                |
| `drawLine()`       | 선                 |
| `drawArc()`        | 호 또는 부채꼴          |
| `drawPoints()`     | 점 또는 여러 선분        |
| `drawPath()`       | 직접 만든 복잡한 경로      |
| `drawImage()`      | `ImageBitmap` 그리기 |
| `drawText()`       | 직접 텍스트 그리기        |
| `drawOutline()`    | `Outline` 그리기     |
| `drawVertices()`   | 정점 기반 고급 그래픽      |
| `drawIntoCanvas()` | 하위 Canvas 직접 접근   |
| `drawLayer()`      | 기록된 그래픽 레이어 그리기   |

---


    // 그리기 영역 정보
    size
    center
    density
    layoutDirection

    // 도형 그리기
    drawCircle()
    drawRect()
    drawRoundRect()
    drawLine()
    drawArc()
    drawOval()
    drawPath()
    drawImage()
    drawText()

    // 좌표 변환
    translate()
    rotate()
    scale()
    inset()
    withTransform()

## 확장함수로도 정의 후 사용 가능
```kotlin

private fun DrawScope.drawBookIcon()
            // ↑ 확장 대상
```