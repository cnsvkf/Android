### 서버 값 돌려받는 방법

---
---
### API
-> 서버에게 무엇을 호출할 지 작성

---
---
### 흐름

```
View (UI)
   ↓
ViewModel (요청 처리)
   ↓
Repository (중간 관리자) // 뷰모델은 로직관리인데 API 쓰면 역할 혼동 때문에  중간관리자 통해서 쓴다
   ↓
Service (API 호출 함수) :  endpoint를 정의하고 실행
   ↓	
Base URL + Endpoint(API의 일부분 : API 안에 있는 하나의 주소(기능)) → 서버 요청 	// Base URL : 서버 공통 시작 주소
 예시) https://api.example.com/) 변하지 않음
    	// Endpoint (API 경로) : 특정 기능을 수행하는 세부 경로

   ↓
Server(백엔드) (JSON 응답) // 플랫폼(언어) 상관없이 코드 볼 수 있게 json 형태
   ↓
Model (DTO로 변환) // 다시 코드 원래대로 변환
   ↓
ViewModel // 데이터 변경 반영 (호출은 View → ViewModel / 데이터 흐름은 ViewModel → View)
   ↓
View (UI 업데이트) // 뷰가 뷰모델의 ‘상태’를 읽고 있어서 다시 적용
```

---
---

### AndroidManifest.xml 필수

```kotlin
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 인터넷 사용 권한 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        ...
    />
</manifest>
```








---
## 각 개발부분

1. Dto : 서버로 보낼 데이터 형식과 받을 데이터 형식 지정

###### 1.5 ApiResult : 서버통신 결과 나누는 파일 

예) 
```kotlin
package com.example.realapp.util

// 서버통신 결과를 성공/실패로 명확히 나누기 위한 sealed class

//out : 밖으로 꺼내는 건 가능하지만 안으로 넣는 건 못함
sealed class ApiResult<out T> {

    // 성공일 때 실제 데이터를 함께 담음
    data class Success<T>(val data: T) : ApiResult<T>()

    // 실패일 때 에러 메시지와 선택적으로 코드도 담음
    data class Error(
        val message: String,
        val code: Int? = null
    ) : ApiResult<Nothing>()
}
```

2. Service : 인터페이스로 구축

3. Retrofit : json변환기등록  및 BaseURL 설정 / 로그확인용 인터셉터 설정 / 실제 네트워크 연결 담당하는 값 만들기

###### 3.5 : 실제 사용할 API 객체 생성

4. Repository : 요청/응답 데이터 가공 예외 처리(404오류 등) 

###### 4.5 UiState : 서버에서 응답받고 UI가 관찰할 상태 만들기

###### 4.5 ViewModelFactory : 뷰모델이 레포생성하기 위해 생성

5. ViewModel : 레포와 뷰(UI) 연결

6. View

### 팁

- 오류가 난다면 ViewModel 같은 것보다 데이터 오류

